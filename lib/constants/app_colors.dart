import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Main background
  static const Color background = Color(0xFF08090D);

  // Slightly lighter dark surfaces
  static const Color surface = Color(0xFF10131A);

  // Glassmorphism background
  static const Color glass = Color(0x33171A22);

  // Glass border
  static const Color glassBorder = Color(0x553A3438);

  // Premium rose-gold accent
  static const Color roseGold = Color(0xFFE9A39B);

  // Soft peach highlight
  static const Color softPeach = Color(0xFFF2B8AE);

  // Main text
  static const Color primaryText = Color(0xFFF5F1EF);

  // Secondary text
  static const Color secondaryText = Color(0xFFB8B0B0);

  // Muted text
  static const Color mutedText = Color(0xFF756D70);

  // White
  static const Color white = Colors.white;

  // Transparent
  static const Color transparent = Colors.transparent;
}